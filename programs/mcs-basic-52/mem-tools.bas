10    SRC=0 : DEST=SRC+255 :  GOSUB 2200
11     PRINT  :  PRINT  :  PRINT 
20    SRC=1000H : DEST=SRC+255 :  GOSUB 2000
21     PRINT  :  PRINT  :  PRINT 
22    SRC=2000H : DEST=SRC+0FFH :  GOSUB 2300
25    SRC=2000H : DEST=SRC+0FFH :  GOSUB 2100
30     END 
230   XBY(ADDR)=(DEST-ADDR)
2000   REM Display 256 bytes of program memory starting at address given by sr
2001   PRINT  SPC (6), :  FOR C=0 TO 15 :  PH0. C, :  NEXT C :  PRINT 
2002   PRINT 
2005   FOR ADDR=SRC TO DEST STEP 16
2010   PH1. ADDR,
2020   FOR COL=ADDR TO ADDR+15
2025   PH0. CBY(COL),
2026  R=(COL+1)-(INT((COL+1)/16)*16)
2027   IF R=0 THEN  PRINT 
2030   NEXT COL
2050   NEXT ADDR
2060   RETURN 
2100   REM Display 256 bytes of external data memory starting at address given
2101   PRINT  SPC (6), :  FOR C=0 TO 15 :  PH0. C, :  NEXT C
2102   PRINT  :  PRINT 
2105   FOR ADDR=SRC TO DEST STEP 16
2110   PH1. ADDR,
2120   FOR COL=ADDR TO ADDR+15
2125   PH0. XBY(COL),
2126  R=(COL+1)-(INT((COL+1)/16)*16)
2127   IF R=0 THEN  PRINT 
2130   NEXT COL
2150   NEXT ADDR
2160   RETURN 
2200   REM Display 256 bytes of internal data memory starting at address given
2201   PRINT  SPC (6), :  FOR C=0 TO 15 :  PH0. C, :  NEXT C :  PRINT 
2202   PRINT 
2205   FOR ADDR=SRC TO DEST STEP 16
2210   PH1. ADDR,
2220   FOR COL=ADDR TO ADDR+15
2225   PH0. DBY(COL),
2226  R=(COL+1)-(INT((COL+1)/16)*16)
2227   IF R=0 THEN  PRINT 
2230   NEXT COL
2250   NEXT ADDR
2260   RETURN 
2300   REM Updates 256 bytes of external data memory with values from 0 to 0ffh
2310   FOR ADDR=SRC TO DEST
2320  XBY(ADDR)=(DEST-ADDR)
2330   NEXT ADDR
2340   RETURN 
